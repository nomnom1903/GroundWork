
  # GroundWork

  This is a code bundle for GroundWork. The original project is available at https://www.figma.com/design/vntHx1dEdlLLKDFWhnjRAw/GroundWork.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  